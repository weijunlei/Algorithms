package match;

/**
 * @author : Qingfeng
 * @date : 2018/6/29
 */
public class Match {
    public boolean match(char[] str, char[] pattern){
        if (str == null || str.length == 0 || pattern.length == 0){

        }
    }
}
